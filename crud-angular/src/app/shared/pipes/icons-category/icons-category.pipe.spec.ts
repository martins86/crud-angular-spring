import { IconsCategoryPipe } from './icons-category.pipe';

describe('IconsCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new IconsCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
